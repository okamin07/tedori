import { MediaIndex } from "@/components/MediaIndex";

export const metadata = {
  title: "読みもの",
  description: "税金・確定申告・フリーランスの基礎知識",
};

export default function MediaPage() {
  return <MediaIndex />;
}
